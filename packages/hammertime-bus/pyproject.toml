[project]
name = "hammertime-bus"
version = "0.1.0"
license = "MIT"
requires-python = ">=3.12"
dependencies = ["hammertime-core", "aiokafka>=0.11"]

[project.scripts]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hammertime"]
