[project]
name = "hammertime-store"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["hammertime-core", "redis>=5.0"]

[project.scripts]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hammertime"]
