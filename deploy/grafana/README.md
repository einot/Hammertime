# Dashboards

`hammertime.json` mirrors the metric groups in §37: ingestion, sliding window,
trie, detection, and the two end-to-end latencies
(`observation_to_hot_transition_latency`,
`hot_transition_to_prefix_update_latency`).
